{
  "name": "ZendeskFunctions",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "@sendgrid/mail": "^7.3.0",
    "lodash": "^4.17.20",
    "node-zendesk": "^2.0.3",
    "twilio-flex-token-validator": "^1.5.4"
  },
  "devDependencies": {
    "twilio-run": "^2.6.0"
  },
  "engines": {
    "node": "10"
  }
}
